{
    "private": true,
    "scripts": {
        "build": "mkdocs build",
        "dev": "mkdocs serve",
        "lint": "remark ./Source/**/*.md --quiet --frail",
        "lint:fix": "remark ./Source/**/*.md --quiet --frail --output"
    },
    "dependencies": {
        "remark-cli": "^10.0.1",
        "remark-preset-lint-mkdocs-material": "^0.6.0"
    }
}
